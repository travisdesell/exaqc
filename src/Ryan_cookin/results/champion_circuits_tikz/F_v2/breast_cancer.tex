% stage F_v2, dataset breast_cancer, seed 0
% D=30 features, N=5 qubits, K=2 classes. enc_gates=3, ansatz_gates=2, test_acc=0.877. enc_rot is a universal 1-qubit data-dependent gate (3 Euler angles, each its own affine function of one input component). enc_xx/yy/zz are 2-qubit Ising rotations whose angle is a linear combination of two input components. x_i (and x_j for 2-qubit gates) in the formulas below are generic input symbols; the legend states which components they resolve to per gate.
% Feature mapping per encoder gate (x_i / x_j -> concrete component of x):
%   gate 381 (enc_rot, q0): x_i = x_0
%   gate 368 (enc_rot, q0): x_i = x_0
%   gate 385 (enc_yy, q1,q0): x_i = x_1, x_j = x_0
% Compile with: pdflatex <this file>
\documentclass[border=6pt,varwidth=40cm]{standalone}
\usepackage{quantikz}
\usepackage{xcolor}
\begin{document}
\begin{quantikz}[row sep={1cm,between origins}, column sep=0.6cm]
\lstick{$q_{0}$} & \gate[style={fill=blue!15}]{R\!\left(\substack{a^{\alpha}_{381} x_i + b^{\alpha}_{381} \\ a^{\beta}_{381}  x_i + b^{\beta}_{381}  \\ a^{\gamma}_{381} x_i + b^{\gamma}_{381}}\right)} & \gate[style={fill=blue!15}]{R\!\left(\substack{a^{\alpha}_{368} x_i + b^{\alpha}_{368} \\ a^{\beta}_{368}  x_i + b^{\beta}_{368}  \\ a^{\gamma}_{368} x_i + b^{\gamma}_{368}}\right)} & \gate[style={fill=orange!15}]{R_x(+0.58)} & \qw & \gate[2,style={fill=blue!15}]{R_{YY}\!\left(a^{0}_{385} x_i + a^{1}_{385} x_j + b_{385}\right)} & \qw \\
\lstick{$q_{1}$} & \qw & \qw & \qw & \gate[style={fill=orange!15}]{R_x(+1.41)} &  & \qw \\
\lstick{$q_{2}$} & \qw & \qw & \qw & \qw & \qw & \qw \\
\lstick{$q_{3}$} & \qw & \qw & \qw & \qw & \qw & \qw \\
\lstick{$q_{4}$} & \qw & \qw & \qw & \qw & \qw & \qw
\end{quantikz}

\vspace{1.5ex}

{\small
\fcolorbox{black!50}{blue!15}{\rule{0pt}{1em}\hspace{1.6em}}~Encoding gates (data-dependent: $a\,x_i + b$) \quad
\fcolorbox{black!50}{orange!15}{\rule{0pt}{1em}\hspace{1.6em}}~Ansatz / learning gates (trained scalar) \quad
controlled gates (cx/cz) keep their native ctrl/target look.\par}
\end{document}
